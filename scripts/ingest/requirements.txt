# Python ingestion dependencies. Pinned to known-good versions so the
# pipeline is reproducible. Use Python 3.11+ in a venv.
#
# Note on openai+httpx: openai 1.55 passes a `proxies` kwarg that httpx 0.28+
# rejects. We use openai 2.x which fixed this, paired with httpx 0.27.x
# which is still widely available on PyPI.

pandas==2.2.3
numpy==2.1.3
openai==2.34.0
httpx==0.27.2
opensearch-py==2.7.1
requests==2.32.3
python-dotenv==1.0.1
tqdm==4.67.1
kaggle==1.6.17
